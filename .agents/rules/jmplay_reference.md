# Laravel Reference (JMPLAY)
When working on Gemflix, if there is any ambiguity about how a feature, UI component, or data model should be structured or behave, ALWAYS reference the legacy Laravel project.

Path to Laravel project: `C:\LAPTOP\JMPLAY`

Use `grep_search` and `view_file` to inspect the `app/Filament` directory, `app/Services`, and `database/migrations` in that directory to match functionality identically or improve upon it.
